.group
  .title Checkpoints
  .content
    %table
      %thead
        %tr
          %th Number
          %th Description
          %th P/N
          %th
      %tfoot
        %tr
          %td
          %td
          %td
          %td
            %ul.icons
              %li= icon_to 'Edit', edit_protocol_path(@protocol)
      %tbody
        = render :partial => 'checkpoints/checkpoint_row', :collection => checkpoints.sort_natural, :as => :checkpoint
