#title= title 'Inspections'

.columns
  .var_left
    = render :partial => 'inspections', :locals => { :inspections => @inspections.pending, :group_title => 'Pending Inspections' }
  .var_right
    = render :partial => 'inspections', :locals => { :inspections => @inspections.closed, :group_title => 'Closed Inspections' }

