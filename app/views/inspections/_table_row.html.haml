%tr{class: cycle('', 'alt')}
  %td= I18n.l i.created_at.to_time, format: :short_date
  %td= i.aircraft_registration
  %td= i.state.capitalize
  %td= i.zone_name
  %td
    %ul.icons
      - if can? :read, i
        %li= icon_to 'Show', i
      - if can? :update, i
        %li= icon_to 'Edit', edit_inspection_path(i)
      - if can? :destroy, i
        %li= icon_to 'Destroy', i, confirm: 'Are you sure?', method: :delete
