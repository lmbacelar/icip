= title 'Edit Location', "Edit Location on |
                         #{File.basename @image.file_url, '.*'}" |

= render 'location_data'

= render 'form', { :target => @location }

.actions
  = link_to 'Back to Image', @image
