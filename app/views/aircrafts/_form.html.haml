= simple_form_for @aircraft do |f|
  = f.error_messages
  .group
    .title Details
    .inputs
      = f.input :registration, :input_html => { :size => 8 }
      = f.input :manufacturer, :input_html => { :size => 12 }
      = f.input :model, :input_html => { :size => 12 }
    .clear
  = f.submit

